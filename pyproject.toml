[project]
name = "re-mcp-servers"
version = "0.1.0"
description = "Reverse engineering MCP servers"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.0"
httpx = "^0.27.0"
